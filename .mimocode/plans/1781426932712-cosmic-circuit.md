# Attendance Monitoring & Payroll Enhancement Plan

## Phase 1: Fix Critical Bugs in Payroll Calculation

### 1.1 Fix `$honorarium` used before definition (CRITICAL)
**File:** `backend/api.php`
- **Line 306:** Move `$honorarium = 0;` to BEFORE line 306 (e.g., after line 257)
- **Line 1273:** Same fix in `run_specialized_payroll` duplicate code

### 1.2 Fix `$daily_rate_util` undefined in Utility payroll (CRITICAL)
**File:** `backend/api.php`
- **Line 369:** Change `$daily_rate_util` to `$rate_per_day` (already defined on line 362)
- This fixes Utility employees always getting $0 holiday pay

### 1.3 Fix night differential + holiday pay double-counted in Faculty gross pay (CRITICAL)
**File:** `backend/api.php`
- **Line 306:** Remove `$holiday_pay` and `$night_diff_pay` from the gross_pay formula since they're already included via `$adj_plus` and `$differential`
- Corrected formula: `$gross_pay = $basic_pay + $load_pay + $overtime + $differential + $substitution + $adj_plus + $honorarium + $total_allowances;`
- **Line 1273:** Same fix in `run_specialized_payroll`

### 1.4 Fix Half-Day status logic (broken for lunch-skipping employees)
**File:** `backend/api.php`
- **Lines 2528-2531:** Change condition to check if BOTH check_in AND lunch_in are empty (not OR), so employees who skip lunch are NOT marked Half-Day
- Better logic: Mark Half-Day only when check_in is empty (arrived very late) or check_out is missing (left very early)

### 1.5 Fix `REPLACE INTO` destroying approval status
**File:** `backend/api.php`
- **Lines 356-358, 470-472, 1321-1323, 1424-1426:** Replace all `REPLACE INTO payroll` with `INSERT INTO payroll ... ON DUPLICATE KEY UPDATE`
- Preserve `approved_by`, `approved_at`, `approval_status` fields on re-runs

### 1.6 Fix `get_attendance` ignoring date filter
**File:** `backend/api.php`
- **Line 2143:** Add date filter to SQL: `AND a.log_date = ?` using the `$date` variable from line 2142
- Add `LIMIT 500` as safety cap

### 1.7 Remove duplicated code block
**File:** `backend/api.php`
- **Lines 1161-1169:** Delete the duplicate block (exact copy of lines 1151-1159)

## Phase 2: Fix Attendance Monitoring Bugs

### 2.1 Fix cron never marking Faculty as Late
**File:** `backend/cron_auto_attendance.php`
- **Lines 51-55:** After INSERT, compare `$current_time_full` against `$sched['time_start']` to calculate late_minutes
- Set status to 'Late' if late_minutes > 0, otherwise 'On-Time'
- Apply company `grace_period` setting

### 2.2 Fix cron check_out time using current time instead of schedule time_end
**File:** `backend/cron_auto_attendance.php`
- **Line 117:** Use `$sched['time_end']` (or the last schedule's time_end) instead of `$current_time_full` for the check_out value

### 2.3 Extend cron to handle Staff and Utility employees
**File:** `backend/cron_auto_attendance.php`
- Add a new section that queries employees with `position IN ('Staff', 'Utility')` and `status = 'Active'`
- Use the company's `work_start` and `work_end` times from the `companies` table
- Auto-create check_in at `work_start` (within window) and check_out at `work_end` (within window)
- Calculate late status against `work_start` with grace period

### 2.4 Fix `log_spoof_attempt` not in API whitelist
**File:** `backend/api.php`
- Add `'log_spoof_attempt'` to the `$allowedActions` array (around line 43-153)

## Phase 3: Add Manual Attendance Entry/Edit UI

### 3.1 Implement `add_attendance` and `update_attendance` API actions
**File:** `backend/api.php`
- Add `case 'add_attendance':` in the switch statement — requires HR role, validates inputs, inserts record with audit log
- Add `case 'update_attendance':` — requires HR role, validates inputs, updates record with audit log

### 3.2 Add manual attendance modal in the frontend
**File:** `js/script.js`
- Add `addAttendance()` function that opens a SweetAlert2 modal with fields: Employee (dropdown), Date, Check-In, Lunch-Out, Lunch-In, Check-Out, Status, Notes
- Add `editAttendance(id)` function pre-filled with existing data
- Wire the modal submit to the new API actions

### 3.3 Wire up View/Flag attendance buttons
**File:** `js/script.js`
- **Lines 2228-2236:** Implement `viewAttendanceDetails(id)` to show a read-only modal with full attendance info
- Implement `flagAttendance(id)` to toggle a flagged status on the record

### 3.4 Add 'Half-Day' to status filter
**File:** `js/script.js`
- **Lines 2125-2130:** Add `{ value: 'Half-Day', label: 'Half-Day' }` to the status filter options

### 3.5 Add Total Hours column to attendance table
**File:** `pages/shared/attendance.php`
- Add a TOTAL HOURS column to the table header and row template

## Phase 4: Payroll Re-run Safety

### 4.1 Add UNIQUE KEY constraint for payroll deduplication
**File:** `sql/complete_schema.sql`
- Add `UNIQUE KEY uk_payroll_period (company_id, employee_id, period, payroll_type)` to the `payroll` table

### 4.2 Convert all `REPLACE INTO payroll` to `INSERT ... ON DUPLICATE KEY UPDATE`
**File:** `backend/api.php`
- Preserve approval-related fields: Do NOT update `approved_by`, `approved_at`, `approval_status` on re-runs
- Only update calculation fields: `basic_pay`, `deductions`, `net_pay`, `total_hours`, `breakdown`, `gross_pay`, gov contributions

## Phase 5: Performance Quick Wins

### 5.1 Fix N+1 query in `get_attendance` schedule enrichment
**File:** `backend/api.php`
- **Lines 2151-2189:** Batch-fetch all Faculty schedules in one query BEFORE the loop, then look up from PHP array

### 5.2 Add database indexes for payroll queries
**File:** `sql/complete_schema.sql`
- Add composite indexes: `employees(company_id, position, status)`, `attendance(employee_id, log_date)`, `payroll(company_id, employee_id, period, payroll_type)`

## Verification Plan

1. **Payroll calculation accuracy:** Run `run_specialized_payroll` for both Faculty and Utility, verify:
   - Holiday pay is non-zero for employees with attendance on holidays
   - Night differential is counted exactly once in gross pay
   - Honorarium appears in gross_pay formula correctly
   - Re-running payroll preserves approval status

2. **Attendance monitoring:** 
   - Test `get_attendance` with date parameter — verify only that date's records return
   - Test manual add/edit attendance via new UI
   - Test `log_spoof_attempt` endpoint works

3. **Cron auto-attendance:**
   - Run `cron_auto_attendance.php` manually and verify Faculty with late arrival get 'Late' status
   - Verify Staff/Utility auto check-in/out works based on company work hours

4. **Edge cases:**
   - Employee with no attendance records in period → should get $0 pay, not error
   - Employee with missing lunch logs → should NOT be marked Half-Day
   - Payroll re-run on already-approved records → approval status preserved
