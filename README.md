# ALM-Biometric-Attendance-Payroll-System-for-ACLC-College-Tacloban
# System Files Only

The Biometric Attendance Payroll System is a collaborative class project undertaken by the BSIT-3A students of [Your College/University Name] under the course Application Lifecycle Management (ALM) . This project serves as the primary academic requirement for the semester, demonstrating the students' ability to apply software development methodologies to solve real-world problems.

The project partners with ACLC College Tacloban, specifically its Payroll Office, to develop a system that automates the integration of biometric attendance data with payroll computation. By addressing the actual needs of a functioning institution, the project provides students with invaluable hands-on experience in requirements gathering, system design, development, testing, and deployment.

---
## **🎨 FRONTEND TEAM**

### **Step-by-Step Upload Process:**

#### **Step 1: Create a Frontend Folder (One-time Setup)**
1. Go to your repository: https://github.com/johnlcstudios/ALM-Biometric-Attendance-Payroll-System-for-ACLC-College-Tacloban
2. Click **"Add file"** → **"Create new file"**
3. Name it: `frontend/.gitkeep` (creates the folder)
4. Click **"Commit changes"**

#### **Step 2: Upload Frontend Files**
1. Go to the **frontend** folder
2. Click **"Add file"** → **"Upload files"**
3. Select your frontend files (HTML, CSS, JS, React components, etc.)
4. Add commit message: `feat: add frontend [component/feature name]`
5. Click **"Commit changes"**

#### **Step 3: Organize by Sub-folders (Recommended)**
Create these structure inside `frontend/`:
```
frontend/
├── components/
├── pages/
├── assets/
├── styles/
└── config/
```

---

## **⚙️ BACKEND TEAM**

### **Step-by-Step Upload Process:**

#### **Step 1: Create a Backend Folder (One-time Setup)**
1. Go to your repository
2. Click **"Add file"** → **"Create new file"**
3. Name it: `backend/.gitkeep`
4. Click **"Commit changes"**

#### **Step 2: Upload Backend Files**
1. Go to the **backend** folder
2. Click **"Add file"** → **"Upload files"**
3. Select your backend files (Python, Node.js, Java, API endpoints, etc.)
4. Add commit message: `feat: add backend [module/feature name]`
5. Click **"Commit changes"**

#### **Step 3: Organize by Sub-folders (Recommended)**
Create this structure inside `backend/`:
```
backend/
├── controllers/
├── models/
├── routes/
├─�� middleware/
├── config/
└── utils/
```

---

## **💡 Alternative: Using Git Command Line (Faster)**

If you have Git installed locally:

### **Frontend Files:**
```bash
git clone https://github.com/johnlcstudios/ALM-Biometric-Attendance-Payroll-System-for-ACLC-College-Tacloban.git
cd ALM-Biometric-Attendance-Payroll-System-for-ACLC-College-Tacloban

# Add your frontend files to the frontend/ folder
git add frontend/
git commit -m "feat: add frontend components"
git push origin main
```

### **Backend Files:**
```bash
# Add your backend files to the backend/ folder
git add backend/
git commit -m "feat: add backend modules"
git push origin main
```

---

## **✅ Best Practices**

| Practice | Description |
|----------|-------------|
| **Use descriptive commit messages** | `feat: add login page` instead of `update` |
| **Keep folders organized** | Group related files together |
| **Update README.md** | Document new features/changes |
| **Use branches** | Create feature branches for larger changes |
| **Code review** | Have team members review before merging |
